--INSERT INTO tasks (task, done)
--VALUES ('Doit', 'N');

--DROP TABLE tasks;

--CREATE TABLE tasks (
--id serial PRIMARY KEY,
--task VARCHAR(100) NOT NULL,
--done VARCHAR(20) NOT NULL
--);

--DELETE FROM tasks 
--WHERE id = 1;

--UPDATE tasks 
--SET done = 'Y' 
--WHERE id = 1;
